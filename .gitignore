# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
.env.*.local

# Lenny's Data archive export — synced into the DB by scripts/sync-lenny-corpus.mjs,
# never committed (their ToS is personal-use-only).
/data/lenny-corpus/
*.lenny-archive.zip

# Claude Code local/machine-specific state — settings.local.json has held a live
# bearer token in an allowlisted command string before; never commit it.
.claude/settings.local.json
.claude/scheduled_tasks.lock